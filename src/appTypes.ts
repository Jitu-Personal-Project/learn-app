export interface LanguageObjTy {
  name: string;
  value: string;
  sound: string;
}
export interface ThemeItem {
  name: string;
  idx: number;
  value: "light" | "dark" | "transparent";
  sound: string;
}
